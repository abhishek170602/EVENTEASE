const userController = {
    async homeRouter(req, res, next){
        try{
            UserActivation.k
        } catch(error){
            return next(error);
        }
    }
}

export default userController;